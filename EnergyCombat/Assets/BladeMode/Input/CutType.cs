namespace BladeMode.Input
{
    public enum CutType
    {
        Joystick,   // right stick drives angle freely
        Horizontal, // L input: force horizontal slice (normal = up)
        Vertical    // H input: force vertical slice   (normal = right)
    }
}
